package com.clipnest.ui.editor

/** Small editor math helpers kept source-compatible with the command layer. */
internal fun max(first: Int, second: Int): Int = kotlin.math.max(first, second)
internal fun min(first: Int, second: Int): Int = kotlin.math.min(first, second)
