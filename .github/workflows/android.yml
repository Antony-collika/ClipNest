name: Android CI/CD

# Disabled automatic GitHub Actions execution.
# CI/CD is being migrated to CircleCI.
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - run: echo "GitHub Actions CI/CD is disabled. Use CircleCI instead."
